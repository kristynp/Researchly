<% if @research_goal %>
  <h1>Add A New Resource for the <%= @research_goal.name %> Goal </h1>
<% else %>
  <h1>Add A New Resource</h1>
<% end %>

<%= render '/errors/error', object: @resource %>

<%= render 'form' %>

