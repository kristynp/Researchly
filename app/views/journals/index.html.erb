<h1>All Journals</h1>
<%= link_to "Add Journal", new_journal_path%>

<ul>
<% @journals.each do |journal| %>
  <li><%=journal.id%><%= journal.name %> | <%= link_to journal.website, journal.website if journal.website %> | <%= link_to "edit", "/journals/#{journal.id}/edit"%></li>
<% end %>
</ul>